name: sports_hub
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  get: ^4.6.6
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  firebase_database:
  firebase_storage:
  image_picker:
  video_player:
  cached_network_image:
  intl:
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
  assets:
    - images/
    - video/
